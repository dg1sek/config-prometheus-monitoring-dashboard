apiVersion: 1

deleteDatasources:
  - name: Prometheus

datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  url: http://prometheus:9090/prometheus # Datasource is in Prometheus container - docker will find it
  isDefault: true
  version: 1
  editable: true
